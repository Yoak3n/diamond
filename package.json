{
  "name": "diamond",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "sidecar": "bash scripts/build-sidecar.sh",
    "sidecar:release": "bash scripts/build-sidecar.sh --release",
    "tauri:dev": "bash scripts/build-sidecar.sh && pnpm tauri dev",
    "tauri:build": "bash scripts/build-sidecar.sh --release && pnpm tauri build"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10",
    "@tauri-apps/cli": "^2"
  }
}
